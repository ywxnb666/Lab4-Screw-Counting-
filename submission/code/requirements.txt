# Generated from current repository imports + conda env: screw_count (Python 3.10.x)
# Merged with previous requirements.txt; requirement.txt content takes precedence.
# Purpose: minimal dependencies to run run.py / count_videos.py with dbscan/incremental dedup.

# Core runtime dependencies
numpy==2.2.6
opencv-python==4.13.0.92
torch==2.10.0
torchvision==0.25.0
ultralytics==8.4.24
scikit-learn==1.7.2

# Optional dependencies
# Pillow is used when SAHI path is enabled in detector (_detect_with_sahi).
Pillow==12.1.1

# SAHI is optional in this repo (run.py uses use_sahi=False by default).
# Install if you want sliced inference on high-res frames:
# sahi==<version in your target env>

# Tooling-only optional dependencies (not required by run.py main pipeline)
# psutil==7.2.2
# tabulate==0.10.0
